/**
 * 提供在原始消息中使用的分数标记的描述。
 */
export interface RawMessageScore {
    /**
     * @remarks
     * 要匹配的分数值名称。
     */
    name?: string;
    /**
     * @remarks
     * 要匹配的分数值名称。
     */
    objective?: string;
}